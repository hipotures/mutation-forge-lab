The whole Native v3 batch was unusable. Repair it using the bounded
diagnostics appended to this request.

Keep exactly the same requested slot IDs and return one complete replacement
batch in the outer `source` string. Do not add replacement slots, Python,
Markdown, or prose outside the required outer JSON object.
