Generate exactly one complete ordinary-Python mutation policy that follows the
supplied M4 contract literally.

Return only the JSON object required by that contract. Do not return Markdown,
explanations, commentary, or additional fields.

Use only the documented runtime interface and documented semantics. Do not
invent APIs, hidden state, graph information, selector scores, or runtime
capabilities. Do not use tools or external runtime context.
